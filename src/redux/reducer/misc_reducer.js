import initialState from '../initial_state.js';

const miscReducer = (state = initialState.miscState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default miscReducer;
